import type { ReactNode } from 'react';

import '../../styles/activa-graphic-system.css';
import '../../styles/activa-icons.css';

export default function UiPreviewLayout({ children }: { children: ReactNode }) {
  return children;
}
